# 张路个人数字空间：Codex 项目说明

本文件是这个项目的长期协作规则。每次开始新任务时，先理解项目当前状态，再进行分析或修改。

## 开始任务前

1. 阅读 `docs/PROJECT_STATUS.md`，确认当前线上状态、已完成事项和下一步计划。
2. 需要了解目录、内容格式或发布方法时，阅读 `docs/README.md`。
3. 需要追溯重要改动时，阅读 `docs/CHANGELOG.md` 和 Git 历史。
4. 检查当前 Git 分支和工作区状态，保留用户已有且与本次任务无关的改动。

## 项目身份与内容边界

- 这是张路的个人数字空间；真实姓名是张鸿钰，公开称呼为“张路”。
- 网站用于发布个人介绍、真实文章和独立项目实践。
- 不编造经历、能力、项目、成果、数字或观点；不确定的事实需要向张路确认。
- 手机、邮箱及其他联系方式默认不公开。只有张路明确指定公开的内容才可以加入网站。
- `.trae/` 是本地旧规划材料，保持本地，不读取为现行需求，不修改，也不加入 Git 提交。

## 内容与代码维护

- 个人资料的主要数据源是 `src/data/profile.ts`。
- 文章位于 `content/posts/`，项目位于 `content/projects/`，内容格式为 MDX。
- 文章编辑以保留作者观点和表达风格为原则；明显改写需要单独说明。
- 示例项目可以暂时保留，但不得把示例内容描述为张路的真实经历或成果。
- 正式域名确定前，继续以 `docs/PROJECT_STATUS.md` 记录的当前地址为准。
- 架构调整前先解释必要性、影响、迁移成本和长期维护成本，不因追求技术复杂度而改造。

## 检查与发布流程

1. 在本地完成修改，并说明改了什么以及可能产生的影响。
2. 与网站功能或内容有关的修改，执行 `npm run check`；纯文档改动至少执行 `git diff --check`。
3. 能本地预览的内容先提供预览或说明验证结果。
4. 未经张路当次明确确认，不得执行 Git 提交、推送 GitHub 或更改线上部署配置。
5. 获得确认后，每次改动形成含义清晰、范围独立的提交，再推送至 `main`。
6. 推送后检查 GitHub 与 Vercel 部署结果；域名相关任务还需检查 DNS、HTTPS 和主要页面。

## 任务结束时

- 重要内容、结构、部署或协作状态发生变化时，更新 `docs/PROJECT_STATUS.md`。
- 值得长期追溯的版本变化写入 `docs/CHANGELOG.md`。
- 向张路说明已完成事项、检查结果、尚未完成事项，以及是否已经上传或上线。
